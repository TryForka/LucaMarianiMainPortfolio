{
  "buildCommand": "pnpm --filter @workspace/drew-halle run build && cp -r artifacts/drew-halle/dist dist",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}
